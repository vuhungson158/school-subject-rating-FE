import {Box} from "@mui/material";
import {LoginPage, UserResign} from "../auth";

export const PopUps = () => {
    return (
        <Box>
            <LoginPage/>
            <UserResign/>
        </Box>
    );
};