import {Box} from "@mui/material";
import {LoginPage, UserResign} from "../features/auth";

export const PopUps = () => {
    return (
        <Box>
            <LoginPage/>
            <UserResign/>
        </Box>
    );
};